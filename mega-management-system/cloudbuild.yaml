# ============================================
# Google Cloud Build Configuration
# Auto-deploys to Cloud Run when pushing to 'qa' branch
# ============================================

steps:
  # Step 1: Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    dir: 'mega-management-system'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/mega-management:$COMMIT_SHA'
      - '-t'
      - 'gcr.io/$PROJECT_ID/mega-management:latest'
      - '.'
    timeout: '1200s'

  # Step 2: Push image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/mega-management:$COMMIT_SHA'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/mega-management:latest'

  # Step 3: Deploy to Cloud Run with environment variables
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'mega-management'
      - '--image'
      - 'gcr.io/$PROJECT_ID/mega-management:$COMMIT_SHA'
      - '--region'
      - 'asia-south1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--memory'
      - '1Gi'
      - '--cpu'
      - '1'
      - '--min-instances'
      - '0'
      - '--max-instances'
      - '3'
      - '--timeout'
      - '300'
      - '--concurrency'
      - '80'
      - '--port'
      - '8080'
      - '--set-env-vars'
      - 'NODE_ENV=production,DISABLE_CRON=true,MONGODB_URI=mongodb+srv://krishna191105:kris461331@cluster0.gp77ive.mongodb.net/mega-management?retryWrites=true&w=majority,JWT_SECRET=mega-jwt-secret-key-2024-production-secure,JWT_EXPIRE=7d,GOOGLE_VISION_API_KEY=AIzaSyBt5AKJZebCbizaqos-Fwn7CAykzh-czhk,GEMINI_API_KEY=AIzaSyDG7aTsLP0tCnibnx1N4qwzaG5GLHjwVYo'

# Store images in Container Registry
images:
  - 'gcr.io/$PROJECT_ID/mega-management:$COMMIT_SHA'
  - 'gcr.io/$PROJECT_ID/mega-management:latest'

# Build timeout (30 minutes total)
timeout: '1800s'

# Build options
options:
  logging: CLOUD_LOGGING_ONLY
  machineType: 'E2_HIGHCPU_8'
